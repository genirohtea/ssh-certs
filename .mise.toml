# MANAGED BY TEMPLATE (copied 2026-05-23 from 6bed4) — do not edit. Sync from the source template; user must manage overwrites.

[env]
# Arbitrary Environment Variables
PROJECT_NAME = 'ssh-certs'

[tools]
# Specify tooling versions
python = '3.12.2'
opentofu = '1.8.1'
# Required by the tofu_tflint / tofu_trivy / tofu_checkov pre-commit hooks.
tflint = 'latest'
trivy = 'latest'
checkov = 'latest'

[plugins]
# specify a custom repo url
# note this will only be used if the plugin does not already exist


[settings]

# END MANAGED TEMPLATE — add custom content below
